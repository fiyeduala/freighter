import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export function InboxPage() {
  return (
    <PlaceholderPage
      screenId="M-01"
      title="Messages"
      description="Threaded conversations list: counterpart name, role badge, last message, unread count, shipment ID."
    />
  );
}
