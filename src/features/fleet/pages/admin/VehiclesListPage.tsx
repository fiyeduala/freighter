import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export function VehiclesListPage() {
  return (
    <PlaceholderPage
      screenId="A-18"
      title="Fleet — Vehicles"
      description="Vehicles list with plate, type, capacity, status, assigned driver, next service."
    />
  );
}
