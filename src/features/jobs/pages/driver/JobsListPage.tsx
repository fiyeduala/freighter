import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export function JobsListPage() {
  return (
    <PlaceholderPage
      screenId="D-02"
      title="Assigned Jobs"
      description="Incoming/queued assignments: pickup, destination, cargo summary, distance, payout, accept-by countdown."
    />
  );
}
