import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export function CreateShipmentWizardPage() {
  return (
    <PlaceholderPage
      screenId="C-02"
      title="Create Shipment"
      description="9-step wizard: Pickup → Destination → Cargo → Vehicle type → Schedule → Review → Quote → Payment → Confirmation."
    />
  );
}
