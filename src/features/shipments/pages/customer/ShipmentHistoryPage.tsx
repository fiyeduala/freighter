import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export function ShipmentHistoryPage() {
  return (
    <PlaceholderPage
      screenId="C-05"
      title="Shipment History"
      description="Completed/cancelled shipments with filters, search, reorder and receipt download."
    />
  );
}
