import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export function ShipmentsListPage() {
  return (
    <PlaceholderPage
      screenId="A-02"
      title="Shipments"
      description="All shipments with status tabs, search, filters, and sortable table."
    />
  );
}
