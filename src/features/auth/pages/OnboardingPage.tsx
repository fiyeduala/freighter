import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export function OnboardingPage() {
  return (
    <PlaceholderPage
      screenId="S-06"
      title="Onboarding"
      description="Role-aware first-run flow: customers add an address, drivers upload documents, admins set org name."
    />
  );
}
