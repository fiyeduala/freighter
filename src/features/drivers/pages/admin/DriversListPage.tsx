import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export function DriversListPage() {
  return (
    <PlaceholderPage
      screenId="A-14"
      title="Drivers"
      description="Driver list with online/offline status, verification, assigned vehicle, rating."
    />
  );
}
