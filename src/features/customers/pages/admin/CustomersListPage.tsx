import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export function CustomersListPage() {
  return (
    <PlaceholderPage
      screenId="A-11"
      title="Customers"
      description="Customer list with name, contact, shipment count, lifetime value, status."
    />
  );
}
