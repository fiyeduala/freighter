import { PlaceholderPage } from "@/components/shared/PlaceholderPage";

export function PaymentsListPage() {
  return (
    <PlaceholderPage
      screenId="A-24"
      title="Payments"
      description="Transactions list with gateway ref, order, customer, amount, method, status, date."
    />
  );
}
